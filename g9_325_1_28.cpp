#include <stdio.h>
int main()
{
    /* Write a program to print first 10 natural numbers using do while. */
    int i=0 ;
    do
    {
        i++;
        printf("%d\n",i);
    }
    while(i<10);
}
